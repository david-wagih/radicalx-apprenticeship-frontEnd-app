import { validateEmail } from './Controller';
import { validatePassword } from './Controller';

//TODO here we make the call to the API but first check validation

export const logIn = (email: string, password: string) => {};
